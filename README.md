# gym-booking-manager
Full-stack gym booking system demonstrating secure backend architecture. Uses PHP and MySQL to manage class capacities and reservations. Key features include handling race conditions via database transactions, preventing SQL injection with prepared statements, and robust server-side input sanitization.
